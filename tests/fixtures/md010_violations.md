# MD010 Test - Hard Tabs

This line has a	tab in it
This line is fine
	Tab at start

## Code Block

```javascript
	// Tab in code (should be flagged by default)
	function test() {
		return true;
	}
```

Normal text again	with tab
